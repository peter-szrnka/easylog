<settings>
  <servers>
    <server>
      <id>jitpack.io</id>
      <username>${env.JITPACK_USER}</username>
      <password>${env.JITPACK_TOKEN}</password>
    </server>
    <server>
      <id>github</id>
      <username>${env.GITHUB_ACTOR}</username>
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>
</settings>